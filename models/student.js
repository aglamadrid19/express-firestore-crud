class Student {
  constructor(id, firstName, lastName, classEnrolled) {
    this.id = id;
    this.firstName = firstName;
    this.lastName = lastName
    this.classEnrolled = classEnrolled;
  }
}

module.exports = Student;